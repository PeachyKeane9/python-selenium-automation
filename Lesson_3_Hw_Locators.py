# Lesson 3 Hw - Locators

Amazon logo, search by Class, $$(".a-icon.a-icon-logo")

Create account text, search by tag + Class, $$("h1.a-spacing-small")

Your name text field, search by tag + ID, $$("input#ap_customer_name")

Email text field, search by tag + ID, $$("input#ap_email")

Password text field, search by tag + ID, $$("input#ap_password")

Password requirement tooltip text, search by tag + ID, $$("div#ap_password_context_message_section")

Re-enter password field, search by tag + ID, $$("input#ap_password_check")

Continue button, search by Tag  + ID, $$("input#continue")

Conditions of use text, search by tag xpath parent child, $x("//div//a[@href='/gp/help/customer/display.html/ref=ap_register_notification_condition_of_use?ie=UTF8&nodeId=508088']")

Conditions of use text, search by tag xpath parent child, $x("//div//a[@href='/gp/help/customer/display.html/ref=ap_register_notification_condition_of_use?ie=UTF8&nodeId=508088']"

Sign In link, search by Tag + Class, $$("a.a-link-emphasis")
